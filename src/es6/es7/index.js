
//Includes
//con este método podemos saber si hay un elemento dentro del objeto o array

let numbers = [1,2,3,5,7,8,9];

if (numbers.includes(7)) {
    console.log('Si está el 7, qué quieres?')
} else {
    console.log ('No está, quién pregunta?')
};

//ELVACIONES A LA POTENCIA

let base = 4;
let exponente = 2;
let result = base ** exponente; //para poner un exponente o elevar, se tienen que poner el doble * " ** "

console.log(result);
