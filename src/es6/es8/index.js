
//OBJECTS

//esto se hace para convertir un objeto en una constante

const data = {
    fronted: 'Oscar',
    backend: 'Isabel',
    design: 'Anna'
}

const entries = Object.entries(data);
console.log(entries);
